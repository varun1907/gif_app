-How to run
Clone the repository
Install expo globally
Run npm i to install dependencies

After dependencies are installed, connect your laptop and mobile on same wifi network.
Install Expo Go app from google play store in your phone.

Run expo start in the cli.
Metro sever will  be started and a qr code will  be shown on terminal .Scan that qr code thorugh expo go app
App will run after this.